from setuptools import setup

setup(name='presh_distributions_for4_pypi',
      version='0.1',
      description='Gaussian distributions',
      packages=['presh_prob_distributions'],
      zip_safe=False)
