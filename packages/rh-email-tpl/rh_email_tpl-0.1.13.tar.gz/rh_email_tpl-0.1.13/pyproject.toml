[tool.black]
line-length = 120
include = '\.pyi?$'
skip-string-normalization = false
exclude = '''
/(
  \.venv
  | migrations
)/
'''

[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"