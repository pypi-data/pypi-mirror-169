from setuptools import setup

setup(name='tbs2022_distributions',
      version='0.1',
      description='TBS Gaussian and Binomial distributions',
      packages=['tbs2022_distributions'],
      zip_safe=False)
