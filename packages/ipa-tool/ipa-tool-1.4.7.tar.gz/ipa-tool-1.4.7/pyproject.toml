[tool.poetry]
name = "ipa-tool"
version = "1.4.7"
description = "A simple tool for apple ipa"
authors = ["DrEgg <dgg152@protonmail.com>"]
license = "GNU"

[tool.poetry.scripts]
ipa-tool = "ipa_tool.main:app"

[tool.poetry.dependencies]
python = "^3.6.4"
lief = "^0.12.1"
typer = "^0.6.1"
rich = "^12.5.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
