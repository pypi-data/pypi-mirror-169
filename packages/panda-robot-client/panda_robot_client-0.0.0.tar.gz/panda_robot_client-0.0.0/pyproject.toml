[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "panda-robot-client"
description = "Python Client for calling the web server for the Franka Emika Panda robot."
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
authors = [
    { name = "William Shen", email = "willshen@mit.edu" },
]
classifiers = [
  "Programming Language :: Python",
  "Programming Language :: Python :: 3.8",
  "License :: OSI Approved :: MIT License",
]
keywords = ["robotics", "panda"]
dependencies = [
    "pyjwt",
    "requests",
    # LISDF
#    "lisdf@git+https://github.com/Learning-and-Intelligent-Systems/lisdf.git",
]
dynamic = ["version"]

[project.urls]
Homepage = "https://github.com/williamshen-nz/panda-robot-client"

[project.optional-dependencies]
lint = [
    "isort",
    "black",
    "mypy",
]
tests = [
    "pytest",
    "pytest-cov",
]

[tool.isort]
profile = "black"
line_length = 120

[tool.black]
line-length = 120
target-version = ['py38']

[tool.hatch.version]
path = "panda_robot_client/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["panda_robot_client"]

[tool.hatch.metadata]
allow-direct-references = true

