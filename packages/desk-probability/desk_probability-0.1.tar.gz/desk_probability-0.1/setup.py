from setuptools import setup

setup(name='desk_probability',
      version='0.1',
      description='Gaussian (a.k.a., Normal) and Binomial probability distributions',
      packages=['desk_probability'],
      author='Sankar Ray',
      zip_safe=False)